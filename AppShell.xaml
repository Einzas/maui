<Shell xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
       xmlns:views="clr-namespace:speed.Views"
       xmlns:local="clr-namespace:speed"
       x:Class="speed.AppShell">
    <TabBar>
        <Tab Title="Inicio"
             Icon="home.png">
            <ShellContent ContentTemplate="{DataTemplate local:MainPage}"/>
        </Tab>
        <Tab Title="Código"
             Icon="code.png">
            <ShellContent ContentTemplate="{DataTemplate views:BarcodeScannerPage}"/>
        </Tab>

    </TabBar>
</Shell>
